<?php

declare(strict_types=1);

namespace PrintAgent\Sdk\DTO\Enums;

/** Mirrors `FontFamily` in the Print Agent (src/document/interfaces/style.types.ts). */
enum FontFamily: string
{
    case A = 'A';
    case B = 'B';
}
